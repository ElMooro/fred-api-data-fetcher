export const parseDate = (dateString: string): Date | null => {try {if (!dateString || typeof dateString !== "string") {return null;}const date = new Date(dateString);return isNaN(date.getTime()) ? null : date;} catch (error) {console.error("Date parsing error:", error);return null;}};export const validateDateRange = (startDateStr: string, endDateStr: string): { startDate: Date, endDate: Date } => {const startDate = parseDate(startDateStr);const endDate = parseDate(endDateStr);if (!startDate || !endDate) {throw new Error("Invalid date format. Please use YYYY-MM-DD format.");}if (startDate > endDate) {throw new Error("Invalid date range. Please ensure start date is before end date.");}return { startDate, endDate };};export const formatDate = (date: Date): string => {try {return date.toISOString().split("T")[0];} catch (error) {console.error("Error formatting date:", error);return "";}};
