import React, {useState, useEffect} from "react";import {WatchlistItemChart} from "./WatchlistItemChart";import {DataService} from "../../services/DataService";import {WebSocketService} from "../../services/WebSocketService";import {IndicatorMetadata, WatchlistItem, DataPoint, Statistics} from "../../types";const Dashboard: React.FC = () => {const [data, setData] = useState<DataPoint[]>([]);const [isLoading, setIsLoading] = useState<boolean>(false);useEffect(() => {setIsLoading(true);setTimeout(() => {setData([{date: "2023-01-01", value: 105.2},{date: "2023-02-01", value: 107.5},{date: "2023-03-01", value: 106.8},]);setIsLoading(false);}, 1000);}, []);return (<div><h1>Financial Dashboard</h1>{isLoading ? (<p>Loading...</p>) : (<div><p>Data points: {data.length}</p></div>)}</div>);};export default Dashboard;
